# 蓝色版本（当前生产）
apiVersion: apps/v1
kind: Deployment
metadata:
  name: gateway-blue
  namespace: aidi
  labels:
    version: blue
spec:
  replicas: 2
  selector:
    matchLabels:
      app: gateway
      version: blue
  template:
    metadata:
      labels:
        app: gateway
        version: blue
    spec:
      containers:
      - name: gateway
        image: your-registry/aidi-gateway:old-tag
        # ... 其他配置